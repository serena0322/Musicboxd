<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/homeFragment">

    <!-- Fragment Home -->
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.musicboxd.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_searchFragment"
            app:destination="@id/searchFragment" />
        <action
            android:id="@+id/action_homeFragment_to_addFragment"
            app:destination="@id/addFragment" />
        <action
            android:id="@+id/action_homeFragment_to_activityFragment"
            app:destination="@id/activityFragment" />
        <action
            android:id="@+id/action_homeFragment_to_profileFragment"
            app:destination="@id/profileFragment" />
    </fragment>

    <!-- Fragment Search -->
    <fragment
        android:id="@+id/searchFragment"
        android:name="com.example.musicboxd.SearchFragment"
        android:label="Search"
        tools:layout="@layout/fragment_search"  />

    <!-- Fragment Add-->
    <fragment
        android:id="@+id/addFragment"
        android:name="com.example.musicboxd.AddFragment"
        android:label="Add"
        tools:layout="@layout/fragment_add" >

    </fragment>

    <!-- Fragment Activity-->
    <fragment
        android:id="@+id/activityFragment"
        android:name="com.example.musicboxd.ActivityFragment"
        android:label="Activity"
        tools:layout="@layout/fragment_activity" >

    </fragment>

    <!-- Fragment Profile -->
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.example.musicboxd.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile" />


</navigation>
